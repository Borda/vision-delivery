#!/usr/bin/env bash
# ponytail: M0 stub — Codex bundled-install mechanism unverified (§15 adversarial log)
# When verified: copy skills/ agents/ .mcp.json into dist/<plugin-name>/ in Codex's expected shape
echo "build-codex-pkg: Codex bundled install mechanism not yet verified — see .plans/active/roboflow-cv-plugin-blueprint.md §15"
echo "Standalone install works now: Codex can consume root skills/ directly via --skills flag"
exit 0
